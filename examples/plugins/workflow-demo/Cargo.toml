[package]
name = "norishell-workflow-demo"
version = "1.0.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
norishell-plugin-sdk = { path = "../../../crates/plugin-sdk" }
serde_json = "1.0.149"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
