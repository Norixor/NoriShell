[[permission]]
identifier = "secure-credential-commands"
description = "Read, answer or cancel the exact SSH credential prompt, and explicitly open the nested Vault prompt when saving it."
commands.allow = ["secure_credential_get", "secure_credential_submit", "secure_credential_cancel", "secure_vault_open"]
