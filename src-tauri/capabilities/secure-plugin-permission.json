{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secure-plugin-permission-capability",
  "description": "Protected special plugin permission window with no access to the main command surface.",
  "windows": ["secure-plugin-permission-*"],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "secure-plugin-special-permission-commands"
  ]
}
