{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secure-ssh-challenge",
  "description": "SSH identity and authentication challenges belong to an exact isolated window.",
  "windows": ["secure-ssh-challenge-*"],
  "permissions": ["core:window:allow-start-dragging", "secure-ssh-challenge-commands"]
}
