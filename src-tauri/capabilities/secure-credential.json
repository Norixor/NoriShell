{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secure-credential",
  "description": "SSH secrets are submitted only by their exact Core-owned credential prompt window.",
  "windows": ["secure-credential-*"],
  "permissions": ["core:window:allow-start-dragging", "secure-credential-commands"]
}
