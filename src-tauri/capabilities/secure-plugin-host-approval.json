{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secure-plugin-host-approval-capability",
  "description": "Protected plugin Host approval window with no access to the main command surface.",
  "windows": ["secure-plugin-host-*"],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "secure-plugin-host-approval-commands"
  ]
}
